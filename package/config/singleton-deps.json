{
  "react": {
    "singleton": true,
    "requiredVersion": "${deps.react}"
  },
  "react-dom": {
    "singleton": true,
    "requiredVersion": "${deps['react-dom']}"
  },
  "ionicons": {
    "singleton": true,
    "requiredVersion": "${deps.ionicons}"
  },
  "@ionic/react": {
    "singleton": true,
    "requiredVersion": "${deps['@ionic/react']}"
  },
  "@ionic/react-router": {
    "singleton": true,
    "requiredVersion": "${deps['@ionic/react-router']}"
  }
}
